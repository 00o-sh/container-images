name: Build Windows Server Disk Image (Packer)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Windows Server version to build'
        required: false
        default: '2022'
        type: choice
        options:
          - '2022'
      runner:
        description: 'Runner to use (self-hosted recommended for KVM)'
        required: false
        default: 'ubuntu-latest'
        type: choice
        options:
          - ubuntu-latest
          - self-hosted

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/windows-server
  PACKER_VERSION: "1.10.0"

jobs:
  build-disk-image:
    runs-on: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
    timeout-minutes: 360  # 6 hours for TCG mode
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Free up disk space (GitHub runners only)
        if: github.event.inputs.runner != 'self-hosted'
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/boost
          sudo apt-get clean
          df -h

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            qemu-system-x86 \
            qemu-utils \
            ovmf \
            genisoimage \
            p7zip-full \
            curl \
            unzip

      - name: Check KVM availability
        id: kvm
        run: |
          if [ -e /dev/kvm ] && [ -r /dev/kvm ] && [ -w /dev/kvm ]; then
            echo "KVM is available - build will be fast!"
            echo "available=true" >> $GITHUB_OUTPUT
            echo "accelerator=kvm" >> $GITHUB_OUTPUT
          else
            echo "KVM is NOT available - using TCG (slow)"
            echo "available=false" >> $GITHUB_OUTPUT
            echo "accelerator=tcg" >> $GITHUB_OUTPUT
          fi

      - name: Find OVMF firmware paths
        id: ovmf
        run: |
          echo "Searching for OVMF firmware..."

          for path in \
            "/usr/share/OVMF/OVMF_CODE_4M.fd" \
            "/usr/share/OVMF/OVMF_CODE.fd" \
            "/usr/share/edk2/ovmf/OVMF_CODE.fd" \
            "/usr/share/qemu/OVMF_CODE.fd"; do
            if [ -f "$path" ]; then
              echo "Found OVMF_CODE at: $path"
              echo "efi_code=$path" >> $GITHUB_OUTPUT
              break
            fi
          done

          for path in \
            "/usr/share/OVMF/OVMF_VARS_4M.fd" \
            "/usr/share/OVMF/OVMF_VARS.fd" \
            "/usr/share/edk2/ovmf/OVMF_VARS.fd" \
            "/usr/share/qemu/OVMF_VARS.fd"; do
            if [ -f "$path" ]; then
              echo "Found OVMF_VARS at: $path"
              echo "efi_vars=$path" >> $GITHUB_OUTPUT
              break
            fi
          done

      - name: Install Packer
        run: |
          if command -v packer &> /dev/null; then
            echo "Packer already installed"
            packer version
          else
            curl -fsSL https://releases.hashicorp.com/packer/${{ env.PACKER_VERSION }}/packer_${{ env.PACKER_VERSION }}_linux_amd64.zip -o packer.zip
            unzip packer.zip
            sudo mv packer /usr/local/bin/
            packer version
          fi

      - name: Initialize Packer
        working-directory: packer-windows-server
        run: packer init windows-server-2022.pkr.hcl

      - name: Download Windows Server 2022 ISO
        working-directory: packer-windows-server
        run: |
          # For self-hosted runners, use a persistent cache directory
          if [ "${{ github.event.inputs.runner }}" == "self-hosted" ]; then
            CACHE_DIR="$HOME/iso-cache"
            mkdir -p "$CACHE_DIR"

            if [ -f "$CACHE_DIR/windows-server-2022.iso" ]; then
              echo "Using cached Windows ISO from $CACHE_DIR"
              ln -sf "$CACHE_DIR/windows-server-2022.iso" windows-server-2022.iso
            else
              echo "Downloading Windows Server 2022 Evaluation ISO to cache..."
              curl -L -o "$CACHE_DIR/windows-server-2022.iso" "https://go.microsoft.com/fwlink/p/?LinkID=2195280&clcid=0x409&culture=en-us&country=US"
              ln -sf "$CACHE_DIR/windows-server-2022.iso" windows-server-2022.iso
            fi
          else
            # GitHub-hosted runners: download directly
            echo "Downloading Windows Server 2022 Evaluation ISO..."
            curl -L -o windows-server-2022.iso "https://go.microsoft.com/fwlink/p/?LinkID=2195280&clcid=0x409&culture=en-us&country=US"
          fi

          SIZE=$(stat -c%s "windows-server-2022.iso" 2>/dev/null || stat -f%z "windows-server-2022.iso")
          if [ "$SIZE" -lt 1000000000 ]; then
            echo "ERROR: ISO is only $SIZE bytes. Download may have failed."
            exit 1
          fi
          echo "Windows ISO size: $(ls -lh windows-server-2022.iso | awk '{print $5}')"

      - name: Download VirtIO drivers ISO
        working-directory: packer-windows-server
        run: |
          # For self-hosted runners, use a persistent cache directory
          if [ "${{ github.event.inputs.runner }}" == "self-hosted" ]; then
            CACHE_DIR="$HOME/iso-cache"
            mkdir -p "$CACHE_DIR"

            if [ -f "$CACHE_DIR/virtio-win.iso" ]; then
              echo "Using cached VirtIO ISO from $CACHE_DIR"
              ln -sf "$CACHE_DIR/virtio-win.iso" virtio-win.iso
            else
              echo "Downloading VirtIO drivers ISO to cache..."
              curl -L -o "$CACHE_DIR/virtio-win.iso" "https://fedorapeople.org/groups/virt/virtio-win/direct-downloads/stable-virtio/virtio-win.iso"
              ln -sf "$CACHE_DIR/virtio-win.iso" virtio-win.iso
            fi
          else
            # GitHub-hosted runners: download directly
            echo "Downloading VirtIO drivers ISO..."
            curl -L -o virtio-win.iso "https://fedorapeople.org/groups/virt/virtio-win/direct-downloads/stable-virtio/virtio-win.iso"
          fi
          ls -lh virtio-win.iso

      - name: Create OEMDRV ISO with VirtIO drivers and autounattend
        working-directory: packer-windows-server
        run: |
          mkdir -p oemdrv-contents
          7z x virtio-win.iso -ooemdrv-contents -y
          cp autounattend.xml oemdrv-contents/

          genisoimage -o oemdrv.iso \
            -V "OEMDRV" \
            -J -r \
            oemdrv-contents/

          ls -lh oemdrv.iso
          rm -rf oemdrv-contents

      - name: Build disk image with Packer
        working-directory: packer-windows-server
        run: |
          echo "Starting Packer build..."
          echo "  Runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}"
          echo "  KVM: ${{ steps.kvm.outputs.available }}"
          echo "  Accelerator: ${{ steps.kvm.outputs.accelerator }}"
          echo "  EFI firmware: ${{ steps.ovmf.outputs.efi_code }}"

          packer build \
            -var "iso_url=windows-server-2022.iso" \
            -var "iso_checksum=none" \
            -var "accelerator=${{ steps.kvm.outputs.accelerator }}" \
            -var "efi_firmware_code=${{ steps.ovmf.outputs.efi_code }}" \
            -var "efi_firmware_vars=${{ steps.ovmf.outputs.efi_vars }}" \
            -var "headless=true" \
            -on-error=abort \
            windows-server-2022.pkr.hcl
        env:
          PACKER_LOG: 1

      - name: Verify output
        working-directory: packer-windows-server
        run: |
          ls -lh windows-server-2022.qcow2
          qemu-img info windows-server-2022.qcow2

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push disk image
        working-directory: packer-windows-server
        run: |
          docker build -f Dockerfile.disk \
            --build-arg DISK_FILE=windows-server-2022.qcow2 \
            --label "org.opencontainers.image.title=Windows Server 2022 Disk Image (Sysprepped)" \
            --label "org.opencontainers.image.description=Pre-installed, sysprepped Windows Server 2022 QCOW2 disk image for KubeVirt. Boots directly to OOBE." \
            --label "org.opencontainers.image.created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" \
            --label "org.opencontainers.image.revision=${{ github.sha }}" \
            --label "org.opencontainers.image.version=2022-disk" \
            -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:2022-disk \
            .

          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:2022-disk | tee push.log
          DIGEST=$(grep -oP 'digest: \Ksha256:[a-f0-9]+' push.log || echo "unknown")

          echo "## Windows Server 2022 Disk Image" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Pre-installed, sysprepped Windows Server 2022 QCOW2 disk image." >> $GITHUB_STEP_SUMMARY
          echo "Runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}" >> $GITHUB_STEP_SUMMARY
          echo "KVM: ${{ steps.kvm.outputs.available }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:2022-disk@${DIGEST}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
